apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: appflowy-storage
  namespace: appflowy
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi # Start with a reasonable size
  # storageClassName: scw-bssd (Scaleway's default) is automatic